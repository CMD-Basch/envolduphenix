{% extends "envol/core.html.twig" %}

{% block body %}

    <!-- BANDEAU TITRE -->
    <div class="row bd-titre bd-bleu">
        <div class="container">
            <h2 id="titre-ic">CONNEXION</h2>
        </div> <!-- container -->
    </div> <!-- row -->

    <!-- CORPS DE PAGE -->
    <div class="container corps-page">
        <!-- Texte -->
        <div class="row justify-content-center">
            <div class="col-sm-6">
                <h5 class="sous-titre">Merci de remplir le formulaire</h5>
                <hr class="hr-titre">
                <br>
            </div> <!-- col -->
        </div> <!-- row -->
        <div class="row justify-content-center">
            <div class="col-sm-4 form-ic">

                {% if error %}
                    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}


                <form action="{{ path("fos_user_security_check") }}" method="post">

                    {% if csrf_token %}
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                    {% endif %}

                    <div class="form-group">
                        <label for="username">E-mail:</label>
                        <input type="text" id="username" name="_username" class="form-control" value="{{ last_username }}" required="required" autocomplete="username" />
                    </div>

                    <div class="form-group">
                        <label for="password">Mot de passe:</label>
                        <input type="password" id="password" name="_password" class="form-control" required="required" autocomplete="current-password" />
                    </div>
                    <div class="checkbox">
                        <label for="remember_me" ><input type="checkbox" id="remember_me" name="_remember_me" value="on" /> Se souvenir de moi</label>
                    </div>
                    <input type="submit" id="_submit" name="_submit" value="Connection" />
                </form>

                <p class="text-right"><small><a href="index.php?p=lostpass">J'ai perdu mon mot de passe.</a><br>
                        <!--Je n'ai pas reçu le mail de validation.--></small></p>

            </div> <!-- col -->
        </div> <!-- row -->
    </div> <!-- container -->


{% endblock %}