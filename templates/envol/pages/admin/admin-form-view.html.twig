{% extends "envol/standard.html.twig" %}

{% block container %}
    <div class="row justify-content-center">
        <div class="col-sm-8">
            <h3 class="titre-page">Admin JDR</h3>
            <hr class="hr-titre">
        </div> <!-- col -->
    </div> <!-- row -->
    <div class="row justify-content-center">
        <div class="col-sm-8">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" class="btn" value="Sauvegarder" />
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}