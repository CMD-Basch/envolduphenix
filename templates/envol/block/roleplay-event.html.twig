{% if event.master == app.user %}
    {% set slotted = 'master' %}
    {% set panel_color = 'card-invites' %}
{% elseif event.isPlayer( app.user ) %}
    {% set slotted = 'player' %}
    {% set panel_color = 'card-invites' %}
{% else %}
    {% set slotted = false %}
    {% set panel_color = 'card-blue' %}
{% endif %}

    <div class="card {{ panel_color }}">
        <div class="card-header">
            <h4>{{ event.name }}</h4>
        </div>
        <div class="card-body">
            <p><strong>Meneur :</strong> {{ event.master }}
                <br>
                <strong>Jeu :</strong> {{ event.game }}<br>
                <strong>Style :</strong> {{ event.style }}<br>
                <strong>Description :</strong><br>
                {{ event.description }}<br>
                <strong>Places libres:</strong> {{ event.freeSlots }} / {{ event.slots }}<br>
                <strong>Joueurs :</strong><br>
                {% for player in event.players %}
                    {{ player.username }}{% if loop.last != true %}, {% endif %}
                {% endfor %}
            </p>
            <p>
            {% if app.user %}
                {% if event.master == app.user %}
                    <!--<a class="btn btn-active " href="#">Modifier</a>-->
                {% elseif not event.isFreeSlots() %}
                    <a class="btn btn-outline-secondary disabled" role="button" tabindex="-1" aria-disabled="true" href="#">Partie pleine</a>
                {% elseif event.isPlayer( app.user ) %}
                    <a class="btn btn-outline-primary btn-table" role="button" data-act="leave" data-table-id="{{ event.id }}" href="#">Quitter</a>
                {% elseif eventuser.isEventTimeFree( event ) %}
                    <a class="btn btn-outline-primary btn-table" role="button" data-act="join" data-table-id="{{ event.id }}" href="#">Rejoindre</a>
                {% endif %}
            {% endif %}
            </p>
        </div>
    </div>
